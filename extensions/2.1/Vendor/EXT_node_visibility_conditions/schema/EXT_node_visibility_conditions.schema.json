{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "EXT_node_visibility_conditions.schema.json",
  "title": "EXT_node_visibility_conditions",
  "description": "Defines the structure of the extension object that is associated with a KHR_node_visibility object to define the conditions for the visibility",
  "type": "object",
  "$ref": "glTFProperty.schema.json",
  "properties": {
    "conditions": {
      "description": "Properties that define the conditions based on which the 'visible' flag becomes 'true'. The names of these properties correspond to the 'dimensions' that are defined in the top-level extension object.",
      "type": "object",
      "additionalProperties": {
        "type": ["string", "number", "boolean" ]
      }
    }
  },
  "required": [
    "conditions"
  ]
}
