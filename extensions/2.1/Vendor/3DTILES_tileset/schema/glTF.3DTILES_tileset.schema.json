{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "glTF.3DTILES_tileset.schema.json",
    "title": "glTF.3DTILES_tileset",
    "type": "object",
    "description": "glTF-level 3DTILES_tileset extension.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "geometricError": {
            "type": "number",
            "minimum": 0.0,
            "description": "A nonnegative number that defines the error, in meters, that determines if the tileset is rendered.",
            "gltf_detailedDescription": "The tileset geometric error. A nonnegative number that defines the error, in meters, that determines if the tileset is rendered. At runtime, the geometric error is used to compute Screen-Space Error (SSE), the error measured in pixels. If the SSE does not exceed a required minimum, the tileset should not be rendered, and none of its tiles should be considered for rendering."
        },
        "extensions": { },
        "extras": { }
    },
    "required": [
        "geometricError"
    ]
}
