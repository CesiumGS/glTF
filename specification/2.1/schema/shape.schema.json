{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "shape.schema.json",
    "title": "glTF Shape Resource",
    "type": "object",
    "description": "Parameters describing a shape used for physics, BVH, or any other use case.",
    "allOf": [ { "$ref": "glTFChildOfRootProperty.schema.json" } ],
    "properties": {
        "type": {
            "description": "Specifies the shape type.",
            "anyOf": [
                {
                    "const": "plane",
                    "description": "A plane centered at the origin in local space, optionally with finite extents, with normal along the +Y axis in local space."
                },
                {
                    "const": "sphere",
                    "description": "A sphere shape defined with a radius equal in all directions with smooth normals facing outwards."
                },
                {
                    "const": "box",
                    "description": "An axis-aligned box with a size per-axis, centered at the origin in local space, with hard normals facing outwards."
                },
                {
                    "const": "capsule",
                    "description": "A capsule shape, centered at the origin in local space, equivalent to the convex hull of two spheres located along the Y axis (in local space) at a specified distance."
                },
                {
                    "const": "cylinder",
                    "description": "A cylinder shape, centered at the origin in local space, equivalent to the convex hull of two circles in the X/Z plane positioned along the Y axis at a specified distance."
                },
                {
                    "type": "string"
                }
            ]
        },
        "sphere": {
            "type": "object",
            "description": "A set of parameter values that are used to define a sphere shape.",
            "$ref": "shape.sphere.schema.json"
        },
        "box": {
            "type": "object",
            "description": "A set of parameter values that are used to define a box shape.",
            "$ref": "shape.box.schema.json"
        },
        "capsule": {
            "type": "object",
            "description": "A set of parameter values that are used to define a capsule shape.",
            "$ref": "shape.capsule.schema.json"
        },
        "cylinder": {
            "type": "object",
            "description": "A set of parameter values that are used to define a cylinder shape.",
            "$ref": "shape.cylinder.schema.json"
        },
        "plane": {
            "type": "object",
            "description": "A set of parameter values that are used to define a plane shape.",
            "$ref": "shape.plane.schema.json"
        },
        "name": { },
        "extensions": { },
        "extras": { }
    },
    "required": [
        "type"
    ]
}
